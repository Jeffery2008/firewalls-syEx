{
  "name": "gemini-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for translating IPTables rules to eBPF using Google's Gemini API",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "hono": "^3.12.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2",
    "wrangler": "^3.25.0"
  }
}
